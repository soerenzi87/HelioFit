# Withings API Credentials
WITHINGS_CLIENT_ID=
WITHINGS_CLIENT_SECRET=

# HealthBridge (integrated health data API)
HB_API_KEY=change-me-to-a-random-api-key

# Firebase (optional, for Xiaomi Scale push-sync via FCM)
# FCM device tokens are now managed automatically via /hb/ingest/:token/devices/register
FIREBASE_SA_PATH=
